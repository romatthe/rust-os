[package]
name = "rust-os"
version = "0.1.0"
edition = "2021"
authors = ["Robin Mattheussen <me@romatthe.dev>"]

[dependencies]

# Dont' unwind on panic, just abort the program
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"